specVersion: "v1beta"
serviceId: "newfile"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "newfile-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
